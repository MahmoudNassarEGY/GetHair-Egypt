@model IEnumerable<GetHair_Egypt.Models.Application>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_UserLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AppDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Client.FName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Clinic.CName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AppDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Client.FName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Clinic.CName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AppID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AppID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AppID })
        </td>
    </tr>
}

</table>
