@model GetHair_Egypt.Models.Application

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_UserLayout.cshtml";
}

<h2>Create</h2>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
<div class="form-horizontal">
    <h4>Application</h4>

    <hr />
    <div class="col-sm-6 col-md-4">
        <div class="thumbnail">
            <img src="@Url.Content(ViewBag.CImage1)" alt="..." width="242" height="200">
        </div>
    </div>
    <div class="col-sm-6 col-md-4">
        <div class="thumbnail">
            <img src="@Url.Content(ViewBag.CImage2)" alt="..." width="242" height="200">
        </div>
    </div>
    <div class="col-sm-6 col-md-4">
        <div class="thumbnail">
            <img src="@Url.Content(ViewBag.CImage3)" alt="..." width="242" height="200">
        </div>
    </div>
    <div class="col-sm-6 col-md-4">
        <div class="thumbnail">
            <img src="@Url.Content(ViewBag.CImage4)" alt="..." width="242" height="200">
        </div>
    </div>
    <div>@ViewBag.Address</div>
    <div>@ViewBag.Description</div>

    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    <div class="form-group">
        <div class="col-md-10">
            @Html.HiddenFor(model => model.UserID, new { @Value = Session["ID"] })
            @Html.ValidationMessageFor(model => model.UserID, "", new { @class = "text-danger" })
        </div>
    </div>

    <div class="form-group">
        <div class="col-md-10">
            @*  @Html.DropDownList("CID", null, htmlAttributes: new { @class = "form-control" })*@
            @Html.HiddenFor(model => model.CID, new { @Value = ViewBag.CID })
            @Html.ValidationMessageFor(model => model.CID, "", new { @class = "text-danger" })
        </div>
    </div>

    <div class="form-group">
        <div class="col-md-10">
            @Html.HiddenFor(model => model.AppDate, new { @Value = DateTime.Now.ToString("dd-MM-yyyy") })
            @Html.ValidationMessageFor(model => model.AppDate, "", new { @class = "text-danger" })
        </div>
    </div>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Apply" class="btn btn-default" />
        </div>
    </div>
</div>
        }

        <div>
            @Html.ActionLink("Back to List", "Index")
        </div>
